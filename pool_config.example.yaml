# copy to pool_config.yaml and tweak if needed
max_loops: 0          # 0 = infinite
sleep_seconds: 2
language_map:
  rust: ["cargo check"]
  python: ["pytest -q {tests}"]
  c: ["make -C {dir} || true"]
copilot:
  host: http://127.0.0.1:11434
  model: qwen2.5-coder:7b
